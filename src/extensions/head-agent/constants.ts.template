// ──────────────────────────────────────────────────────────────
// Auto-generated by pi-org-bootstrap — DO NOT EDIT MANUALLY
// To regenerate: npx pi-org-bootstrap init --extensions
// ──────────────────────────────────────────────────────────────

import * as path from "node:path";
import * as os from "node:os";
import type { TeamDef } from "./types.js";

// ─── Memory Mandate ─────────────────────────────────────

export const MEMORY_MANDATE = `
## Memory (MANDATORY)
Your memory file: .agents/agent-memory/{team}.md
This is auto-loaded at the start of every task (first 1500 chars).

IMPORTANT: After completing your task, you MUST use the write tool to APPEND to your memory file with:
- What you accomplished
- Key decisions and rationale
- Gotchas, patterns, or things the next session should know
Format entries as: [YYYY-MM-DD] brief summary of learnings

### Multi-Instance Memory Protocol
When multiple pi instances are active, follow this protocol to avoid data loss:
1. **Read before write** — Always read the current memory file before appending.
2. **Tag with instance context** — If you know your instance ID, include it in entries:
   Format: [YYYY-MM-DD] [instance:<YOUR_INSTANCE_ID>] brief summary
3. **Merge on conflict** — If the file changed since you last read it (different content),
   the memory-merge module will deduplicate by content hash. Trust the merge.
4. **Never overwrite** — Always APPEND, never write the full file unless creating it.

Failure to update memory will cause context loss for future tasks.`.trim();

// ─── Team Definitions ─────────────────────────────────────
// Generated from pi-org-bootstrap stack analysis + user config.
// Each team maps to an agent .md file in .pi/agents/

export const TEAMS: Record<string, TeamDef> = {{TEAMS_OBJECT}};

export const TEAM_ORDER = {{TEAMS_ARRAY}} as const;

// ─── Debug Constants ──────────────────────────────────────
// These are generic — scopes are derived from the team list at runtime

export const DEBUG_SCOPES = {{DEBUG_SCOPES_ARRAY}};
export const DEBUG_STATE_FILE = "debug/state.json";
export const DEBUG_LOG_FILE = "debug/log.md";
export const DEFAULT_MAX_FIXES_PER_CYCLE = 3;
export const DEFAULT_MAX_CONSECUTIVE_ERRORS = 5;
export const DEFAULT_OBJECTIVE_REVIEW_CYCLE = 5;
export const DEFAULT_SLEEP_BETWEEN_CYCLES = 30000;
export const NO_FINDING_SKIP_THRESHOLD = 3;
export const FIX_ONLY_QUEUE_THRESHOLD = 20;
export const DEFAULT_CONTEXT_BUDGET = { l1Wiki: 800, l2AgentMemory: 600, l3RecentMemory: 10, l3Summary: 500, iterationPrompt: 2000 };

export const PHASE_ORDER: import("./types.js").DebugPhase[] = ["scanning", "triaging", "fixing", "testing", "reviewing", "reflecting"];
export const USER_SIM_CYCLE_INTERVAL = 5;

export const MAX_MAILBOX_SIZE = 100;

// ─── Cron Scheduler Constants ────────────────────────────

export const CRON_STATE_FILE = path.join(os.homedir(), ".pi", "agent", "cron.json");
export const CRON_LOG_FILE = path.join(os.homedir(), ".pi", "agent", "cron.log");
export const CRON_DEFAULT_MAX_CONSECUTIVE_FAILURES = 5;
export const CRON_DEFAULT_TASK_TIMEOUT = 30_000;
export const CRON_DEFAULT_FAILURE_COOLDOWN = 60_000;
export const CRON_MAX_HISTORY = 100;

// ─── Multi-Instance Coordination Constants ─────────────

export const REGISTRY_FILE = "instance-registry.json";
export const HEARTBEAT_INTERVAL_MS = 30_000;
export const STALE_THRESHOLD_MS = 120_000; // 2 minutes
export const TASK_KEY_PREFIX = "task:";
